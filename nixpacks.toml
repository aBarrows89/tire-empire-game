[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd client && npm install && npx vite build && cd ../server && npm install"]

[phases.build]
cmds = ["mkdir -p server/public && cp -r client/dist/* server/public/"]

[start]
cmd = "cd server && node index.js"
